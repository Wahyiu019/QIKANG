const fs = require('fs'); const cwPath = 'src/components/ChatWindow.tsx'; let cwContent = fs.readFileSync(cwPath, 'utf8'); const anchor2 = 'text.includes(烟草企业，多机构，预算池已知，历史赔付不完整，帮我准备首次拜访材料)'; let parts = cwContent.split(anchor2); console.log(parts.length); let replacedContent = parts[0]; for(let i=1;i<parts.length;i++){let suffix=parts[i]; let endIdx = suffix.indexOf('return;'); console.log('endIdx:', endIdx);} 